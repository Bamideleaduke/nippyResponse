import React, { useEffect } from "react";
import { Text, View } from "react-native";

const Services = ({ navigation }: any) => {
  //   useEffect(() => {
  //     // Get parent navigator, which is the DrawerNavigator
  //     const parentNavigation = navigation.getParent();
  //     if (parentNavigation) {
  //       // Use toggleDrawer or openDrawer to open the drawer
  //       parentNavigation.toggleDrawer(); // This opens/closes the drawer
  //     }
  //   }, [navigation]);

  return (
    <View>
      <Text>Services screens</Text>
    </View>
  );
};

export default Services;
